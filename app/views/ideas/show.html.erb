<%= @idea.title %>
<%= @idea.body_text %>

<%= link_to "Add Image", new_user_ideas_image_path(@idea.user_id) %>
<%= link_to "Edit", edit_user_idea_path(@idea.user_id, @idea) %>
<%= link_to "<< Back to Ideas", user_ideas_path(@idea.user_id) %>
